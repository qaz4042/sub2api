# 主干能力迁移代办

## 基线与原则

- 基线：`main`；来源：`codex/before-fork-changed`；目标：从 `main` 新建定制分支。
- 旧分支相对 `main` 有 70 个提交、约 329 个文件差异，禁止整体合并或按顺序批量 `cherry-pick`。
- `main` 的代码和目录结构是准绳；旧分支中的删除、目录搬迁和热点文件大改不自动继承。
- 纯重构舍弃；新增能力按模块重做，只保留一个最小公共接入点；不部署生产和测试环境。

## 一、创建新分支

- [x] 确认本地 `main` 是本次目标基线，并保留旧分支只读作为参考。
- [x] 创建新分支（`codex/migrate-openai-privacy`）：

```bash
git fetch --all --prune
git switch main
git switch -c <new-branch-name>
```

- [x] 记录旧分支提交清单和当前差异，后续每项能力单独建立迁移提交。
- [x] 先确认 `main` 是否已包含同类上游实现，避免重复迁移 GPT-5.6、快捷登录、订阅等能力。

执行记录：基于本地 `main`（`e316ebf5`）创建分支；旧分支仅作只读参考。已完成能力分别提交，未部署、未推送。

## 二、按能力迁移

| 优先级 | 旧分支参考提交 | 迁移事项 | 处理方式 |
| --- | --- | --- | --- |
| P0 | `c1e7cc74`、`b128c633`、`f04daccc`、`42620c4d`、`0db90aa0`、`7f6548c8`、`cb4193bd` | OpenAI/Codex 版本、GPT-5.6、推理强度、usage 元数据修复 | 已完成首个缺失子闭环：Codex OAuth 的 `function_call`/`mcp_tool_call` 非字符串 `arguments` 归一化及 Go 回归测试；GPT-5.6、Codex `0.144.1`、`max` 推理强度和 usage 候选列表等其余差异已核对由当前 `main` 覆盖，不重复迁移。 |
| P0 | `dad97ce4` | 隐私状态和重试行为 | 已完成：按当前 `main` 的 OpenAI 服务边界补充边缘拦截/上游失败分类、管理端错误响应和回归测试。 |
| P1 | `302574fc`、`f77c412a`、`888a10b4`、`80ea0e34`、`a9da9be1`、`ebcb6200` | 多域名快捷登录、OAuth 配置和登录/注册触发 | 已完成最小闭环：抽为配置驱动的 auth provider；公共设置、OAuth start/callback 和嵌入式首屏按来源隔离；默认无白名单时保持主干单域名行为。后台多客户端编辑 UI 暂不迁移。 |
| P1 | `10cf6e7e`、`56e53abf` | 平台配置、平台访问控制、ccswitch 导入优化 | 已完成最小闭环：追加 `174` 迁移；后台 API 与设置页支持平台开关；核心平台保护、网关 403、缓存失效和 CCS 地址优先级均有测试与说明。 |
| P1 | `fd9aa633`、`eec2f638`、`3ef11b6e`、`042443c1` | 用量排行、日期筛选、监控健康度 | 已完成最小闭环：复用主干用量排行；统一管理员/用户日期预设；健康分补充低样本、主要信号和 TTFT 参考元数据及展示。 |
| P1 | `11798ea9`、`86dea9a6`、`ceea312d` | 账号套餐剩余时间查询 | 已完成最小闭环：复用 `main` 的订阅/过期时间模型，新增 OpenAI OAuth 套餐查询、管理端 action、前端操作菜单和测试；不刷新 OAuth token。 |
| P2 | `d64db63f`、`e4e2ef45`、`dcc7c70c`、`a63dd632`、`eae6332e` | 使用密钥 UI、品牌、人民币显示、繁中和兑换记录 | 已完成公共支付币种符号、在线支付记录区分、多密钥选择、静态 API 示例、快速实测和模型预设；密码修改 UI 与品牌配置已确认由 `main` 提供，繁中完整语言包暂缓。 |
| P2 | `56e53abf`、`0485c98a` 及部署相关提交 | my4g、Cloudflare、nginx、备份和本地启动脚本 | 只迁移通用模板与脱敏 runbook 到 `deploy/private/`；删除真实域名、生产配置、快照和主机专用逻辑。 |

每项能力完成后：

- [x] 默认关闭扩展时，行为与 `main` 一致。
- [x] 配置、权限、错误路径和回滚方式有说明。
- [x] 代码、测试、文档使用同一能力名，并保持单一意图提交。

已完成提交：

- `4ee243fa fix: report openai privacy setting failures`
- `fbb9b943 feat: refresh openai subscription metadata`
- `397d3a61 feat: support multi-domain email oauth`
- 本次提交：平台访问控制、平台配置和 CCS 导入地址优化。
- `d975df4d fix: normalize Codex tool call arguments`：补齐 Codex OAuth 工具调用参数类型归一化及 Go 回归测试。
- `179b8ec0 fix: update messages dispatch sonnet default`：修正 OpenAI Messages Dispatch 的 Sonnet 默认模型为当前可用的 `gpt-5.4`，保留显式旧映射兼容，并同步管理端默认表单与提示文案。

最近已完成的迁移改动：

- 增加 GitHub/Google OAuth 来源白名单和按来源覆盖客户端配置，支持环境变量 JSON 注入与配置校验。
- 公开设置、登录/注册快捷入口和嵌入式前端首屏按精确 `scheme://host` 过滤，未知来源 fail-closed。
- 补充配置、服务、handler、嵌入式前端回归测试及脱敏部署示例；未迁移数据库多客户端后台 UI，不部署环境。

- 追加平台配置迁移，提供管理端平台开关和网关访问控制；OpenAI 保持核心平台，CCS 导入支持专用地址回退配置。
- 用量/监控项已完成日期筛选子闭环：管理员与用户用量页共享“今天、昨天、本周、本月、上月、自定义”预设和日期计算工具，补充前端回归测试。
- 用量/监控项已完成健康度子闭环：健康评分保留原有分值逻辑，新增低样本置信度、主要信号、业务/基础设施子分数和 TTFT 参考值，健康卡按观察中/上游体验展示。
- 人民币显示已完成最小闭环：支付金额输入框复用当前支付方式币种符号，默认使用 CNY；密码、品牌和繁中继续拆分。
- 兑换记录已完成最小闭环：按支付订单生成的 `PAY-` 充值码区分在线支付和手动兑换，未改变后端历史接口。
- 多密钥选择已完成最小闭环：使用密钥弹窗复用当前列表数据切换密钥，配置示例随选中密钥更新，不新增接口。
- 静态 API 示例已完成最小闭环：按平台展示可复制的 curl 请求和对应鉴权头，不在浏览器内发起真实请求。
- API 快速实测已完成最小闭环：按平台提供预设模型，使用最小请求支持成功、失败、取消和响应截断；切换模型、密钥或关闭弹窗会中止请求。
- 密码修改 UI 与品牌能力已完成基线核对：当前 `main` 已包含 `ProfilePasswordForm`、`siteName/siteLogo` 及安全 URL/SVG 渲染，不重复迁移旧分支大提交。
- 部署项已完成通用模板小闭环：新增脱敏发布/备份/健康检查/回滚 Runbook 和镜像 tag/digest Compose 覆盖文件；不包含真实服务器配置，也未执行部署。
- P0 OpenAI/Codex 子闭环已完成：`function_call` 和 `mcp_tool_call` 的对象/数组 `arguments` 序列化为 JSON 字符串；已有字符串参数和普通消息保持不变。

验证记录：`go test -tags=unit ./internal/service ./internal/handler/admin -count=1`、前端账号操作菜单 Vitest、`pnpm run typecheck` 均通过。

P0 工具参数子闭环验收记录：定向 Codex OAuth 转换测试和 `go test -tags=unit ./internal/service -count=1` 均通过；未修改前端、数据库或部署配置。

Messages Dispatch 默认映射验收记录：未配置 Sonnet 映射时回退到 `gpt-5.4`；已配置的 `gpt-5.3-codex` 等显式值保持不变；相关 Go 测试、前端 Vitest 和 `vue-tsc --noEmit` 均通过。

## 三、明确舍弃

- [x] 舍弃 `666e2cee`、`34d69054` 的 gateway / OpenAI gateway 大规模文件拆分；这是纯重构，后续按需在新主干上小步拆分。
- [x] 不直接搬运 `851d0935`、`9b3a1b40`、`bfe0c50d`、`e4550310` 的大规模文档清理和目录迁移；只挑选仍有效的内容重新归档。
- [x] 不继承旧分支删除的上游功能、合作方资源和 handler/service 文件；以 `main` 当前能力为准。
- [x] 不提交 `.local-data`、真实域名/服务器配置、生产调试快照、安装包和临时产物；`.gitignore` 只保留必要规则。
- [x] `eec2f638`、`10cf6e7e`、`d4909276` 等混合提交不整体搬运，先拆出通用修复、私有能力和文案改动。

## 四、验收清单

- [x] `git diff --name-status main...<new-branch-name>` 已逐项审查，无删除项；热点文件按独立能力拆分提交。
- [x] 后端按能力运行相关 Go 测试；前端运行相关 Vitest 和 `pnpm --dir frontend run typecheck`。
- [x] 涉及嵌入式前端、配置或部署模板时，已补充配置/静态校验和文档核对。
- [x] 已覆盖扩展能力的成功、失败、取消、空响应、权限拒绝和默认行为回归；不执行生产或测试环境部署。
- [x] 完成后回看[开源更新与能力扩展手册](开源更新与能力扩展手册.md)，确认目录、接入点和提交边界一致。

本项验收记录：后端 `go test -tags=unit ./internal/config ./internal/service ./internal/handler ./internal/web -count=1`、`go test -tags=embed ./internal/web`、`go test -tags=unit ./internal/server/routes -run 'Test' -count=1` 均通过；`git diff --check` 通过。通用扩展治理手册已补齐并与当前目录、接入点和提交边界核对一致。

平台访问控制本项验收记录：迁移 `174_platform_configs.sql` 默认全部启用；核心平台不可关闭；禁用平台网关请求返回 `403`；管理端更新后访问控制缓存立即失效；CCS 导入地址按专用地址、API 地址、当前站点顺序回退。相关 Go 测试、前端 Vitest、`pnpm run typecheck` 和 `git diff --check` 均通过。

最终验收记录：`go test -tags=unit ./internal/config ./internal/service ./internal/handler ./internal/web -count=1`、`go test -tags=embed ./internal/web`、`go test -tags=unit ./internal/server/routes -run 'Test' -count=1` 均通过；前端全量 `Vitest` 为 `144` 个测试文件、`934` 个用例通过，`UseKeyModal` 目标测试为 `11` 个用例通过，`vue-tsc --noEmit` 和 `git diff --check` 通过；`docker-compose -f deploy/docker-compose.local.yml -f deploy/private/docker-compose.release.yml config` 使用占位镜像校验通过。未执行任何生产或测试环境部署，`.local-data/` 未提交。

## 五、执行前最终建议

- [x] 先逐项核对 `main` 的等价实现；GPT-5.6、Codex 版本、推理强度等已存在的能力只补缺失差异，不重复迁移。
- [x] 平台配置迁移使用当前 `main` 的下一个唯一编号（当前不得使用 `154`），并验证迁移前后平台默认状态一致。
- [x] 不一次性搭建完整扩展架构；先用一个低风险能力验证最小 `contract`、`registry` 或 `slot` 接入点。
- [x] 将跨层事项继续拆成“一个用户结果、一个最小 API、对应权限和测试”的独立闭环，避免按旧分支提交整体搬运。
- [x] 将“默认关闭”改按能力验收：核心修复验证兼容性，私有功能验证开关/权限，数据库变更验证向前兼容和追加迁移；执行前先隔离现有未提交改动。
